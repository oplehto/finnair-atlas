{
  "name": "airline-atlas",
  "version": "0.1.0",
  "private": false,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "npm run build && node server.mjs",
    "test": "node --test",
    "build": "node build.mjs",
    "deploy": "npm run build && wrangler deploy",
    "preview": "npm run build && wrangler dev"
  },
  "dependencies": {
    "@fontsource/oswald": "^5.3.0",
    "@fontsource/roboto-condensed": "^5.3.0",
    "d3-force": "^3.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "wrangler": "^4.131.1"
  },
  "license": "MIT",
  "description": "An unofficial weekly Finnair network timetable in the style of the 1974 printed sheet"
}
